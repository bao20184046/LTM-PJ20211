#include <stdio.h>

int main(int argc, char const *argv[])
{
	int b;
	printf("1.Fold(Bo bai)--2.Bet(Cuoc)--3.Call(Theo)--4.Raise(To them)--5.All-in(To tat)--6.Check(Xem):  ");
	scanf("%d",&b);
	return 0;
}